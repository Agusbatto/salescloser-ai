/**
 * Placeholder. Cuando exista el esquema en Supabase, reemplazar este
 * archivo con el resultado de:
 *
 *   supabase gen types typescript --project-id <project-id> > src/types/database.ts
 *
 * Mientras tanto, se define un tipo mínimo para no romper los imports
 * de los clientes de Supabase.
 */
export type Database = Record<string, unknown>;
