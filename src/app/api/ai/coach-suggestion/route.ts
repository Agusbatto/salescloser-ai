// Placeholder: endpoint para sugerencias puntuales del coach de IA
// sobre una conversación o contacto específico. Sin lógica todavía (fase 2).
export async function POST() {
  return new Response(null, { status: 501 });
}
